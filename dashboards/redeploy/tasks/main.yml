---

- name: Stop dashboards service
  become: yes
  service:
    name: dashboards
    state: stopped
  delegate_to: "{{hosts_to_remove}}"
  register: dashboards_service_status
  until: dashboards_service_status.status.ActiveState == "inactive"
  retries: 30
  delay: 5
